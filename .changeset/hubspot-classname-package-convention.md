---
"@memberjunction/connector-hubspot": patch
---

ClassName now follows the catalog convention (== npm package name) so instance discovery matches and the catalog card flips to installed; legacy 'HubSpotConnector' key stays registered and a delta migration fixes existing tenants' Integration rows.
